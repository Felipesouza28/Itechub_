# Itechub — Login

A transição da tela de login/cadastro usa a mesma lógica visual da referência:
o painel colorido desliza entre os dois lados enquanto os formulários permanecem no lugar.

- `#show-signin` -> estado de login
- `#show-signup` -> estado de cadastro
- Desktop: movimento horizontal
- Mobile: movimento vertical
